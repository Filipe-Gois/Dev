//union types: colocar tipos diferentes na mesma variavel

let cursos: (string | number)[] = ["a", 1, "ts"]; //--exemplo avançado

// let cursos:string[] = [];

cursos.push("C++");

console.log(cursos);
