//coleção MAP

//diferente do .map dos arrays

//chave => valor
let mapa = new Map();

mapa.set(1, {
  nome: "filipe",
  idade: 17,
});

mapa.set(2, {
  nome: "filipe",
  idade: 17,
});

console.log(mapa.values());
